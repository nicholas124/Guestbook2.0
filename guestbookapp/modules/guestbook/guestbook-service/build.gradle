dependencies {
	compile project(":modules:guestbook:guestbook-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../guestbook-api/src/main/java"
}

group = "docs.guestbook"